using UnityEngine;

namespace UniGreenModules.UniNodeActors.Runtime.Actors
{
    public class GenericActor<TModel> : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
