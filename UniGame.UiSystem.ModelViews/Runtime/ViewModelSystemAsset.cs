using UnityEngine;

namespace UniGame.UiSystem.ModelViews.Runtime
{
    public class ViewModelSystemAsset : MonoBehaviour
    {
        // Start is called before the first frame update
        private void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
